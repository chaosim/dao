简单，易用，灵活，强大的解析器，规则求解器
嵌套式的解析
  函数状态机，正规表达式 rtn，atn，触发条件，发送消息，连接方法？
     发布方与订阅方，pipeline(流水线)
     黑板 消息

sub(x, 1) 成为x-1, 又可以正常地使用运算符了。
-char：可以定义成optional(char) 漂亮
+char: 可以定义成some(char)
char[:] char重复任意次（包括0次）
char[:]/','逗号分隔的列表
char*5: char重复五次
char[5]: char重复5次
char[:5] char小于五次
char[5:] char至少五次


2011-9-13 1:31
sexpression，求解对象
let, letrec, lambda: 作为语法糖，用宏实现
用continuation编译
function和macro的关系？能否取消一个？function是不是宏的特例？好像不行，因为还要合一。

能不能不修改核心？
可选参数：函数调用时，先匹配非可选参数，然后将剩余的参数打包
默认参数，函数体头部增加设置默认值的语句
关键字参数，
可选参数，默认参数，关键字参数都不会导致合一失败。

python的默认参数实现方法一定有问题，应该再执行函数的时候设置默认值。实现应该也不复杂。

如何不写编译器就能实现编译？
pypy能吗？continuation能有多大帮助？ 